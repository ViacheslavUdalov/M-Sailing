services:

  mongo:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: password
  
  
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

volumes:
  redis-data: